name: test workflow
on: push
jobs:
  first_job:
    runs-on: ubuntu-latest
    steps:
      - name: wlecome mesage
        run: echo "welcome to github actions"
      - name: second step
        run: echo "this is the second step"